# 知识库项目设计 Skills 总结

## 项目定位

本项目是一个个人知识库、AI 工作台与虚拟 OS 桌面的组合型产品。

它不是普通资料库，而是一个「个人操作系统式知识入口」：

- 首页以 MacBook 启动页作为第一视觉入口。
- 进入后呈现可操作的 OS 虚拟桌面。
- 作品集承载文件、分类、PDF、链接收藏与无限画板。
- 我的 OS 接入 AI 无限画布能力。

项目目标是形成一个可持续扩展的个人 AI 知识操作系统。

## 整体技术

项目主体采用轻量静态网页方案：

- HTML
- CSS
- JavaScript
- Ant Design v6 前端组件库标准
- 本地文件预览与静态文件部署
- 宝塔面板 + Nginx 静态站点
- GitHub 仓库存档
- 浏览器本地存储保存部分交互状态

03「我的 OS」内嵌的 AI 无限画布基于开源项目 `chatfire-AI/huobao-canvas` 适配，其主要技术栈为：

- Vue
- Vite
- 前端静态构建产物

这种技术组合的优点是部署轻、访问快、服务器压力低，适合作为个人知识库 MVP 与持续迭代型产品。

其中 Ant Design v6 是后续前端组件化与 UI 重构的调用标准；当前静态 MVP 保持现有实现，新增或重构组件时再按该标准逐步接入。

## 前端组件库标准

本项目目前暂定 `Ant Design v6` 作为前端组件库标准。

后续前端新增或重构时，遵守以下原则：

- 新增按钮、表单、下拉选择、弹窗、抽屉、标签页、表格、卡片、提示、上传等组件时，优先按 Ant Design v6 的组件语义和调用标准设计。
- 表单类交互参考 Ant Design 的 `Form`、`Input`、`Select`、`Upload`、`Switch`、`Checkbox` 等模式。
- 弹窗类交互参考 Ant Design 的 `Modal`、`Drawer`、`Popconfirm`、`Message`、`Notification` 等模式。
- 后台管理页面和未来组件化前端优先使用 Ant Design 组件体系。
- 当前静态 MVP 页面不强制立即整体重写为 Ant Design，但后续新增组件的结构、状态和交互标准应向 Ant Design v6 对齐。
- 保留本项目既有浅灰蓝 OS 视觉语言，不直接套用默认主题导致风格割裂。
- 不引入新的第三方 UI 组件库替代 Ant Design，除非后续明确调整项目标准。

## 页面结构

产品主结构分为三个标签页：

| 标签 | 页面 | 核心功能 |
| --- | --- | --- |
| 01 | 主页 | MacBook 启动页、OS 虚拟桌面、桌面文件管理 |
| 02 | 作品集 | 文件管理、分类、PDF、链接收藏、无限画板 |
| 03 | 我的 OS | AI 无限画布，全屏内嵌使用 |

标签栏采用移动 App 风格：

- 图标 + 文字
- 激活态使用浅蓝增强色
- 悬停时图标与文字为白色
- 切换时增加类似 App 底部导航的动效

## 视觉标准

整体视觉统一为浅色、浅灰蓝、轻系统感风格。

### 色彩

- 主色：浅蓝、灰蓝、高纯度蓝
- 背景：柔和浅蓝渐变
- 辅助色：白色、浅灰、低饱和边框色
- 避免过重的深色、杂色与强烈营销风格

### 卡片

- 使用浅色卡片
- 圆角适中
- 轻边框
- 柔和阴影
- 不使用过重的装饰性渐变

### 按钮

- 常规状态：白底或浅灰蓝底，黑字或深灰字，文字居中
- 悬停状态：蓝底，白字，文字居中
- 绿色按钮统一改为浅灰蓝/高纯度蓝体系

### 弹窗

- 文件窗口居中显示
- 使用类似 Mac 窗口的顶部红黄绿圆点
- 内容区保持清爽留白
- 保存按钮常规黑字居中，悬停蓝底白字居中

## 交互标准

项目交互尽量模拟真实桌面系统体验。

### 首页启动

- 首次进入显示 MacBook 启动页。
- 点击 `Press Enter to Launch` 后进入 OS 虚拟桌面。
- 滚动时 MacBook 与桌面整体缩放过渡。
- 缩放过渡过程中使用纯色逐渐显示内容，避免画面抖动。

### OS 桌面

- 桌面文件始终保持右侧排列。
- 文件支持拖拽。
- 支持批量拖拽。
- 支持框选。
- 框选后只有被选中的文件显示选中态。
- 选中态颜色加深。
- 删除按钮在需要时显示，避免常态干扰。

### 文件窗口

- 文件点击后在界面中间居中打开。
- 编辑功能放在文件打开后的窗口内，不直接暴露在桌面上。
- 文件支持打开、编辑、保存。

## 文件能力

当前文件系统能力包括：

- 新建文件
- 编辑文件
- 保存文件
- 删除文件
- 拖拽文件
- 批量拖拽文件
- 框选文件
- 分类管理
- PDF 格式支持
- 通过文章链接或收藏链接生成结构化文件
- 链接内容抓取失败时提供结构化兜底

01 主页和 02 作品集都接入了新建文件逻辑。

## 02 作品集

02 作品集页主要承载作品文件、分类文件与无限画板。

### 文件区

- 增加添加文件入口
- 支持 PDF
- 支持文件点击编辑
- 文件长按后右上角显示删除按钮
- 新文件可以拖入下方分类文件中

### 分类区

- 分类文件可点击打开查看
- 分类文件长按后显示删除按钮
- 删除按钮默认隐藏

### 无限画板

02 作品集里的无限画板是单独功能，不与 03「我的 OS」绑定。

功能方向：

- 独立入口
- 卡片模板选择
- 可新建图层
- 图层与画板卡片对应
- 配置选中卡片与画板卡片对应
- 三者标题保持一致
- 修改卡片标题时同步更新 Layer 名称
- Layer 可点击索引到对应卡片位置
- 卡片之间支持连线，类似 workflow

## 03 我的 OS

03「我的 OS」内嵌 AI 无限画布。

已做适配方向：

- 基于 `chatfire-AI/huobao-canvas`
- 去除火宝相关品牌和图标
- 隐藏 GitHub 图标与跳转链接
- 全屏显示
- 统一按钮颜色为浅灰蓝/高纯度蓝
- 输入框内增加模型选择
- 模型配置和模型选择可下拉
- API Key 采用个人配置方式
- 对 API 接入做防泄露、安全提示与基础检测

## AI 与模型配置

AI 无限画布支持模型配置与选择。

设计原则：

- 不在前端硬编码敏感 API Key。
- API Key 应由用户自行配置。
- 前端只保存必要配置，避免泄露。
- 免费模型或免费额度模型可以作为默认推荐。
- 输入区和配置区都应能选择已配置模型。

可支持方向：

- OpenRouter 免费路由
- DeepSeek 免费模型或兼容模型
- Qwen 免费额度模型
- Gemini 免费额度模型
- Hugging Face 免费额度模型
- 图片模型按可用 API 再接入

## 部署链路

当前部署链路：

1. 本地项目生成 `outputs` 静态产物。
2. GitHub 仓库存档。
3. 宝塔面板上传静态包。
4. 解压到站点目录 `/www/wwwroot/w-shawn.cn`。
5. Nginx 直接访问静态页面。
6. 域名访问：`w-shawn.cn`。

当前线上已部署为知识库项目，旧摄影站文件已清理。

## 开发与测试标准

后续继续开发时遵守以下标准：

- 前端组件新增或重构，按 Ant Design v6 组件库标准执行。
- 新功能只测试本次新增或修改的功能。
- 不需要每次全盘回归。
- 测试产生的文件必须删除。
- 不破坏已有功能。
- 视觉风格保持浅灰蓝系统感。
- 文件、弹窗、按钮、卡片保持统一规范。
- 所有新交互必须实际点击验证。
- 上线后使用无缓存链接验证，例如 `?v=版本号`。
- 如果涉及 API Key，不在代码中明文保存。

## 缓存与上线验证

上线后浏览器旧标签页可能因为缓存不立即更新。

推荐验证方式：

- 使用无缓存参数访问：`http://w-shawn.cn/?v=版本号`
- Mac Chrome 强制刷新：`Command + Shift + R`
- 同时检查：
  - `http://w-shawn.cn/`
  - `http://www.w-shawn.cn/`
  - `http://w-shawn.cn/ai-terminal-kb.html`
  - `http://w-shawn.cn/huobao-canvas/index.html`

## 一句话总结

这个项目本质上是一个个人 AI 知识操作系统：用静态网页承载桌面、文件、作品集和 AI 画布，用轻量部署方式快速上线，用统一 OS 风格保证长期可扩展。
