# Clink AI 项目开发规则

本文件适用于整个仓库。系统、平台和运行环境的强制规则优先；目录中更具体的 `AGENTS.md` 可补充局部约束，但不得降低安全、验证和交付要求。

## 1. 工作原则

- 始终使用中文沟通，明确区分已验证事实、合理推断和需要确认的事项。
- 项目状态以当前代码、文档、Git 状态和实际运行结果为准，不凭历史记忆直接编码。
- 先读取最接近需求的文件，不为追求上下文长度扫描无关目录。
- 用户已明确目标且风险可控时直接执行；存在互斥选型、需求歧义、不可逆操作或外部副作用时才请求确认。
- 严格按用户明确需求和参考图实施，不得以“优化”为由私自增加用户未要求的功能、控件、遮罩、流程或交互；认为确有必要时必须先说明并取得确认。
- 不编造模块、组件属性、模型 ID、API、数据或测试结果。引用前确认文件存在且接口真实导出。

## 2. 开发入口

- 修改前先读 `docs/ARCHITECTURE.md`，通过 `scripts/project-map.js` 判断所属层和变更影响。
- 业务组件优先修改 `src/`；独立页面交互修改 `scripts/standalone/client/`；样式修改 `scripts/standalone/styles.js`。
- 不直接编辑 `outputs/` 实现功能；发布产物由构建命令生成。
- 不在 `vendor/` 内混入 Clink AI 业务代码；适配层放在 `src/` 或 `scripts/`。
- 预览服务的通用 HTTP 能力放在 `scripts/server/`，领域 API 再按稳定边界逐步拆分。
- 不覆盖或回滚用户已有改动；工作区不干净时只修改任务必需文件。

## 3. 前端标准

- 新增或修改的前端组件统一使用 Ant Design v6；智能体对话统一使用 Ant Design X。
- 图标优先使用 `@ant-design/icons`，不重复手绘已有标准图标。
- 所有弹窗默认居中，弹窗内滚动不得带动底层页面。
- 页面文字不得被边框截断、溢出或与控件重叠；必须覆盖空数据、加载、错误、断网和超长文本状态。
- UI 文案使用面向最终用户的产品语言，不把需求原句、开发过程或对话上下文写入界面。
- 动效应使用 `transform` 和 `opacity` 等低开销属性，并遵守 `prefers-reduced-motion`。

## 4. 高效验证

- 每次只测试本次新增或修改的功能，不做无关全盘回归。
- 常规修改运行 `npm run check:changed`。
- 发版前运行 `npm run check` 和 `npm run health`。
- 影响单文件页面时，确保 `npm run build:standalone` 成功并检查内联脚本。
- 影响预览服务时，至少验证 `/api/health` 和目标 API；不得仅凭语法通过声称功能可用。
- UI 修改后刷新当前已打开的预览标签页，不新开页面，并检查目标 DOM、交互和控制台错误。
- 每次开发完成并验证后，将当前预览恢复到本次开发对应的页面/Tab，不停留在启动初始页，确保用户打开即可继续检查刚完成的功能。
- 每次修改完成后必须在真实预览中按用户使用路径完整操作一次，覆盖打开、输入、提交/取消、切换页面等本次涉及的关键步骤；必须以实际界面结果确认成功，构建通过、静态检查或代码推断不能替代使用测试。
- 功能开发完成后，必须实际使用并确认“功能可用且需求已实现”再交付；不得仅凭代码修改、构建通过或静态检查宣称完成。若真实操作未成功，继续排查修复并重新测试；确实无法测试时，必须明确说明未测试原因、已验证范围和剩余风险。
- 测试完成后删除测试文件、临时记录和临时服务，不污染正式数据。
- 未能执行的验证必须说明原因、风险和下一步，不使用“应该可以”作为结论。

## 5. 数据、安全与外部操作

- 密钥只放在本地环境变量或服务器私密配置，不写入源码、日志、截图、文档或 Git。
- 运行数据与个人/官方模型 API 配置不得被构建或发版流程覆盖。
- 发送消息、提交表单、上传、删除正式数据、改变权限、安装软件、提交 Git、推送或部署前，必须有针对该行为的明确授权。
- 默认采用最小权限、最小影响、可逆操作；临时授权不能视为长期授权。

## 6. 任务闭环

- 开始非简单任务时简要说明目标、已核验上下文、影响范围和验证方式。
- 在形成方案、完成关键改动、发现风险或验证失败时给出简洁进度更新。
- 每次被纠正时定位根因，并在同类问题再次发生前补充自动检查或项目规则。
- 修改完成后更新 `updatelog.md`，按时间倒序记录修改文件、核心内容和验证结果。
- 最终交付必须包含：完成状态、核心修改、关键文件、实际验证命令与结果、可点击预览地址。
- Git 提交与推送仅在用户明确要求后执行；执行前展示变更清单并推荐提交说明。

## 7. 规则溯源

- 完整工作流见 `docs/WORKFLOW_RULES.md`。
- 原始参考文件见 `docs/reference/CODEX-AGENTS.pdf`。
- 本项目既有架构、预览和测试约定继续有效；冲突时采用更具体、可验证且不扩大权限的规则。
