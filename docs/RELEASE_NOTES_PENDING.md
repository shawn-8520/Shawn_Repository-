# 待发版改动

以下内容已同步到 GitHub，尚未部署到服务器，将在下次正式发版时一并发布。

## 2026-07-31 代码分层与开发效率优化

- 将原本超过 5,000 行的单文件构建脚本拆分为样式、启动、智能体桌面、无限画板、文档窗口等独立模块。
- 保留 `scripts/build-standalone.js` 作为轻量装配入口，降低定位与修改成本。
- 无限画板资源使用内容哈希作为版本号，连续构建结果稳定，避免无改动产物反复变化。
- 增加 `npm run check:changed`，按当前改动范围执行定向检查。
- 增加 `npm run health`，统计项目层级、文件规模和工作区状态。
- 增加 `.ignore`，开发检索默认排除依赖、第三方源码、发布产物、缓存和备份目录。
- 增加架构、开发流程和换机恢复文档。

## 发版前检查

```bash
npm ci
npm run check:changed
npm run build:standalone
```

完成服务器部署后，再从本文件移除已发布条目或转入正式版本日志。
