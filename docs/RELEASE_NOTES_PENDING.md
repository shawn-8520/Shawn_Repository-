# 待发版改动

## 管理员认证

- 管理员登录改为由 Node API 校验，不再依赖浏览器本地种子账号。
- 账号、密码和签名密钥从服务器私有环境读取，不提交到 GitHub。
- 管理员令牌使用服务端 HMAC 签名并设置有效期，默认拒绝旧版可伪造的成员令牌。
- 修复 Windows 环境下定向检查无法启动 npm 子进程的问题。

已发布内容见 `docs/RELEASE_NOTES_2.1.1.md`。
